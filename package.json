{
  "name": "miniprogram-ts-quickstart",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "gulp css",
    "test": "jest",
    "test:cloud": "npm run test --prefix cloudfunctions/syncCounterData && npm run test --prefix cloudfunctions/syncDiagramData && npm run test --prefix cloudfunctions/login && npm run test --prefix cloudfunctions/deleteUser"
  },
  "keywords": [],
  "author": "",
  "license": "",
  "dependencies": {
    "@vant/weapp": "^1.11.7"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "gulp": "^5.0.1",
    "gulp-postcss": "^10.0.0",
    "jest": "^30.3.0",
    "miniprogram-api-typings": "^4.1.0",
    "postcss-px2units": "^0.2.0",
    "ts-jest": "^29.4.9",
    "typescript": "^6.0.2"
  }
}
